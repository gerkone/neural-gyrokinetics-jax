"""gyrowave.compress — pure-JAX linear transform + physics solves + per-trajectory
token-cache builder (HL velocity moments x (s,x) db4 wavelet, flux-GN + phi-aware).
NO torch, NO neugk (the torch repo)."""
