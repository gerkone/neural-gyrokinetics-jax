batch_size: 4
n_epochs: 1500
learning_rate: 3.0e-4
final_learning_rate: 3.0e-6
weight_decay: 1.0e-4
clip_grad: true
clip_to: 1.0
exclude_from_wd: []
save_every_n_epochs: 500   # rolling ckp.eqx cadence (best.eqx still saved on improvement)
