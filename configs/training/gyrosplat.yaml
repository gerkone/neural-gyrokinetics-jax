batch_size: 32
n_epochs: 200
learning_rate: 1.0e-4
final_learning_rate: 1.0e-6
weight_decay: 0.01
clip_grad: true
clip_to: 1.0
exclude_from_wd: []
save_every_n_epochs: 25   # rolling ckp.eqx cadence (best.eqx still saved on improvement)
